# Lists

- lists can also have lists init
  ex :-x = [[5,6], [2,3]]
  x[0][0] gives 5
- -1 refers to last element in the lists

# Getting sublists with slices

- slice can get several vals from list in form of a new list.
- A slice is tped bw sq brackets lise an index, but it has two ints separated by a colon.
- spam[2] is a list with an index(one integer).
- spam[1:4] is a list with a slice(two integer).
- spam[0:-1] except last elem

# Sting concaat and replication

- \* for replication
- \+ for replicaiton
- del to delete a val from list => del spam[2]

# iterating thro lists

```
x = [1, 2, 5,  6]
for i in x:
    print(i)
```

# check whether element is in list

```
>>>7 in x
    False
>>> 1 in x
    True
```

# Multiple assignment trick

```
cat = ['fat', 'black', 'loud']
size = cat[0]
color = cat[1]
disposition = cat[2]
size, color, disposition = cat
```
