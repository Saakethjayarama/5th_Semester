# shutil

- shutil.copy(path1:src , path2:dest)
- shutil.copytree(src, dest) -> copies entire folder(ie, folders subfolders, files)
- shutil.move(src, dest) -> moves and returns absolute path
- if the destinatino folder doen't exit then it throws exception
- os.unlink(path) -> permanent del
- os.rmdir(path) -> dels dir
- shutil.rmtree() -> premanently deletes files

# 3rd party module send2trah

- send2trash.send2trash('bacon.txt')
