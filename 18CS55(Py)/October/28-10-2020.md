* .groups() -> returns tuples of all grps
* ? -> optional

# Greedy and non greedy matching
```
greedyRegex = re.compile(r'(JIT){3, 5}')
returns max no of reps

nongreedyRegex = re.compile(r'(JIT){3, 5}?') <- question mar used
returns min no of reps
```

# shorthand chars
```
\d num digits 0 - 9
\D any char that is not numeric digit from 0-9
\w any lettter, numberc digit, or the underscore character
\W any char that is not a letter, numberic digit, or the underscore
\s any space tab or newline char
\S any character that is not space, tab or newline.
[aeiouAEIOU] -> own character classes
[a-zA-Z0-9] -> ranges
[^a-z] -> negative of character class
(r'^Hello')-> starts with Hello
(r'\d$') -> ends with number (exactly one no)
(r'^\d$') -> starting and ending with number nothing in btwn
. wild card char -> match character except for a new line
    (The cat in the hat sat on the flat mat) => [cat, hat, sat, lat, mat] lat coz period can match only one character
sec arg to re.compile as re.DOTALL -> mathces all characters, including the newline character.
```