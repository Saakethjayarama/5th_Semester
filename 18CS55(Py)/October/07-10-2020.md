```
# string manipulation example

def manipulation(param):
    n = len(param)

    if n < 2:
        return param+'$'
    return param[:2]+param[-2:]

```

```
single from 2 gvn strng
and swap first two chars of str

def foo(str1):
    x, y = str1.split(' ')
    x = swap(x)
    y = swap(y)
    rturn x + y

def swap(str):
    x, y = temp[:2]
    x, y = y, x
    temp = str[2:]
    return x + y + temp

```

```
write py prog to add ing at en len shld be atleast 3
if ing exist add ly
if len off string is less than 3 return


def foo(str):
    n = len(str)
    if n < 3:
        return str
    elif str[-3:] == 'ing':
        return str+'ly'
    else:
        return str+'ing
```

```
insert string in middle of string

def insert(x, y):
    a = x[x:len(x)//2]
    b = x[len(x)//2:]
    return a+y+b
```

```
def deleteOdd(str):
    final = ''
    for i in range(len(str)):
        if i%2 != 0:
            final += str[i]
    return final
```

```
py fun str made of 4 cpies of two last char of specified str
python => onononon

def 4cp(str):
    return str[-2:]*4

```

```
def pali(str):
    str1 = reversed(str)
    if str1 == str:
        return True
    return False
```
