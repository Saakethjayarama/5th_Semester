# rtrn stmt

- return keywork
- value or eprsn that the fn shld return
- returning None

# print args

- end :- appends the specified chars
- sep :- sep is used to separate multiple print strings

# local and global sscope

- params and vars inside fn local scope
- outside fn global scope

# global stmt

- if global scope then global var
- if global stmt for that var in a fn it is global var
- if var in AT stmt in fn it is local var
- else global

# Exception handling

```
try:
    return 42/ divby
except ZeroDivisionError:
    print('Enter : Invalid arg.')

```
