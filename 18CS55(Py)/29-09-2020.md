# Methods on list

- extend(list)
- append(element)
- insert(index, element)
- remove(element)
- sort(), param :- reverse=True(sorts in desc order), key=str.lower (lowercase appears first then uppercase)
  - if sorted with strs and numbers -> Type err
