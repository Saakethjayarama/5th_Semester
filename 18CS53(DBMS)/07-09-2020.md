# Unique

- this ensures all the rows in column are unique

# primary key

- unique and not null

# Foreign key

- reference between two tables
- refers to primary key in other table
- prim key table parent table other one child table

# ondelete cascade and onupdate cascade

foreign key references person(id) on delete cascade
foreign key references person(id) on update cascade

# check

```
age int,
CHECK(age > 18)
```

- naming constraints

```
constraint chk_person check(age>=18 and city='sandnes')
```

- default constraints

```
cirty varchar(255) default 'bangalore';
```
