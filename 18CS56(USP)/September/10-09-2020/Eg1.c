#include <stdio.h>

int main()
{
    int a, b, c;
    scanf('%d', &a);
    scanf('%d', &b);
    c = a + b;
    print("c = %d\n", c);
    // to execute ./eg5 < input.txt
    // to redirect op to file ./eg5 < input.txt > output.txt
}